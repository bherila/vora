# Testing Environment
# This file ensures tests NEVER connect to production databases

APP_NAME="Ben Herila Tests"
APP_ENV=testing
APP_KEY=base64:2fl+K9kGZYSY6ytAtpKr6EonZcgOkInGo9pWun/clVs=
APP_DEBUG=true

# CRITICAL: Force SQLite in-memory for all tests
# This prevents accidental modifications to real databases
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Clear any MySQL/PostgreSQL settings that could be inherited
DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=

# Use test-safe drivers
CACHE_STORE=array
CACHE_DRIVER=array
SESSION_DRIVER=array
QUEUE_CONNECTION=sync
MAIL_MAILER=array
BROADCAST_CONNECTION=null

# Disable optional features during tests
PULSE_ENABLED=false
TELESCOPE_ENABLED=false
NIGHTWATCH_ENABLED=false

BCRYPT_ROUNDS=4
